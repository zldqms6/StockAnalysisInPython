# 자막 추출
youtube-transcript-api>=1.0.0
yt-dlp>=2024.1.0

# 음성인식 (자막이 없는 영상의 오디오를 텍스트로 변환)
# faster-whisper는 ffmpeg가 설치되어 있어야 가장 잘 동작합니다.
#   macOS:  brew install ffmpeg
#   Ubuntu: sudo apt install ffmpeg
faster-whisper>=1.0.0

# Claude API 요약
anthropic>=0.40.0
